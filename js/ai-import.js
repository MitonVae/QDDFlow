{
  "title": "QDD鍚嶇О锛堜换鍔＄嚎鍚嶇О锛?,
  "steps": [
    {
      "name": "0.鐜妭鍚嶇О",
      "taskType": "STORY",
      "trigger": "Room瑙﹀彂",
      "location": "鍦烘櫙鍚嶇О",
      "characters": "NPC鍚嶇О锛岀敤閫楀彿鍒嗛殧",
      "desc": "杩欎釜鐜妭鐨勮缁嗘弿杩帮紝鍖呮嫭鐜╁瑕佸仛浠€涔堛€佸墽鎯呭唴瀹规憳瑕佺瓑銆?,
      "images": [],
      "customFields": [
        { "key": "鑷畾涔夊瓧娈靛悕", "value": "瀛楁鍐呭" }
      ]
    }
  ]
}

銆愬瓧娈佃鏄庛€?
鈻?title锛堝繀濉級锛氭暣鏉′换鍔＄嚎鐨勫悕绉帮紝濡?绗竴绔犅锋搴峰ぇ妤?

鈻?steps锛堝繀濉級锛氱幆鑺傛暟缁勶紝姣忎釜鐜妭鍖呭惈浠ヤ笅瀛楁锛?
  鈥?name锛堝繀濉級锛氱幆鑺傜紪鍙?鍚嶇О锛屽缓璁牸寮?"0.鐜妭鍚?锛屽 "0.浠诲姟鎺ュ彇"
  
  鈥?taskType锛堝繀濉級锛氫换鍔＄被鍨嬶紝鍙兘鏄互涓嬪洓涓€间箣涓€锛?      - "PUZ"   鐜╂硶鐜妭锛堣В璋溿€佹綔琛屻€佷簰鍔ㄧ瓑锛?      - "BAT"   鎴樻枟鐜妭
      - "NAV"   璺戝浘/瀵艰埅鐜妭锛堢函绉诲姩銆佹帰绱㈠湴鍥撅級
      - "STORY" 鍓ф儏鐜妭锛堝璇濄€佽繃鍦哄姩鐢汇€乼imeline锛?      - ""      锛堢┖瀛楃涓诧級琛ㄧず鏈垎绫?      
  鈥?trigger锛堝彲閫夛級锛氳Е鍙戞柟寮忥紝寤鸿浠庝互涓嬮€夐」閫夊彇锛堜篃鍙嚜瀹氫箟锛夛細
      "Room瑙﹀彂" / "鎺ョ画鑷姩瑙﹀彂" / "鍓ф儏瑙﹀彂" / "鐜╁閫夋嫨" / "鎺ョ画"
      
  鈥?location锛堝彲閫夛級锛氬彂鐢熷湴鐐?鍦烘櫙鍚嶇О锛屽 "姝﹀悍澶фゼ路澶у巺"
  
  鈥?characters锛堝彲閫夛級锛氭秹鍙婄殑瑙掕壊/NPC锛屽涓敤閫楀彿鍒嗛殧锛屽 "闂ㄥ崼寮犱笁, 涓昏"
  
  鈥?desc锛堝彲閫変絾閲嶈锛夛細鐜妭鎻忚堪锛岃鏄庣帺瀹惰涓恒€佸墽鎯呭唴瀹广€佽璁℃剰鍥剧瓑锛屽彲澶氳
  
  鈥?images锛堝彲閫夛級锛氱暀绌烘暟缁?[] 鍗冲彲锛屽浘鐗囬€氳繃宸ュ叿鎵嬪姩涓婁紶
  
  鈥?customFields锛堝彲閫夛級锛氳嚜瀹氫箟鍙傛暟鏁扮粍锛屾瘡椤规牸寮?{"key":"瀛楁鍚?,"value":"鍐呭"}
      甯哥敤绀轰緥锛?      - PUZ绫诲瀷鐨勭帺娉曪細{"key":"鐜╂硶绫诲瀷","value":"瑙ｈ皽/娼滆"}
      - BAT绫诲瀷鐨勬€墿锛歿"key":"鏁屼汉","value":"鏅€氬＋鍏得?"}
      - 濂栧姳鍐呭锛歿"key":"濂栧姳","value":"閬撳叿路閽ュ寵"}
      - 鍒嗘敮閫夐」锛歿"key":"鍒嗘敮","value":"A.閫夋嫨鍚堜綔 / B.閫夋嫨瀵规姉"}

銆愭敞鎰忎簨椤广€?
1. 鍙緭鍑?JSON锛屼笉瑕佹湁浠讳綍棰濆鐨勮В閲婃枃瀛楋紙鍙互鍦?JSON 鍓嶅悗鐢ㄤ唬鐮佸潡鍖呰９锛?2. 鎵€鏈夊瓧绗︿覆鍊间笉鑳藉寘鍚湭杞箟鐨勫紩鍙?3. desc 瀛楁涓殑鎹㈣鐢?\\n 琛ㄧず
4. 涓嶉渶瑕佸～鍐?id銆乧olor銆乮mageUrl銆乧olorOverride 绛夊瓧娈碉紝宸ュ叿浼氳嚜鍔ㄧ敓鎴?5. 鎸夋祦绋嬮『搴忔帓鍒?steps锛岀紪鍙蜂粠 0 寮€濮?
銆愮ず渚嬭緭鍑恒€?
\`\`\`json
{
  "title": "绗竴绔犅锋搴峰ぇ妤?,
  "steps": [
    {
      "name": "0.浠诲姟鎺ュ彇",
      "taskType": "STORY",
      "trigger": "Room瑙﹀彂",
      "location": "姝﹀悍澶фゼ路澶у巺",
      "characters": "绁炵浜?,
      "desc": "鐜╁杩涘叆姝﹀悍澶фゼ澶у巺锛岃Е鍙戣繃鍦哄姩鐢汇€傜绉樹汉鍑虹幇骞跺鎵樼帺瀹跺鎵惧け韪殑妗ｆ銆?,
      "images": [],
      "customFields": []
    },
    {
      "name": "1.澶у巺鎺㈢储",
      "taskType": "NAV",
      "trigger": "鎺ョ画鑷姩瑙﹀彂",
      "location": "姝﹀悍澶фゼ路涓€妤?,
      "characters": "",
      "desc": "鐜╁鍦ㄥぇ鍘呰嚜鐢辨帰绱紝鍙笌闂ㄥ崼瀵硅瘽鑾峰彇绾跨储锛屽彂鐜板悗闂ㄥ叆鍙ｃ€?,
      "images": [],
      "customFields": [
        { "key": "鍙氦浜掔墿浠?, "value": "鍏憡鏍忋€佺數璇濅涵銆佸墠鍙扮數鑴? }
      ]
    },
    {
      "name": "2.浼鐢靛伐",
      "taskType": "PUZ",
      "trigger": "鐜╁閫夋嫨",
      "location": "姝﹀悍澶фゼ路鍚庨棬",
      "characters": "闂ㄥ崼鏉庡洓",
      "desc": "鐜╁闇€瑕佹壘鍒扮數宸ユ湇骞朵吉瑁呮垚缁翠慨浜哄憳锛岄€氳繃闂ㄥ崼鐨勬鏌ヨ繘鍏ュ唴閮ㄣ€俓\n澶辫触鎯╃綒锛氶噸鏂板紑濮嬫綔琛屻€?,
      "images": [],
      "customFields": [
        { "key": "鐜╂硶绫诲瀷", "value": "浼/娼滆" },
        { "key": "閫氬叧鏉′欢", "value": "鎴愬姛楠楄繃闂ㄥ崼" }
      ]
    }
  ]
}
\`\`\`

---
鐜板湪璇锋牴鎹垜鎻愪緵鐨勫唴瀹癸紝鎸変笂杩版牸寮忚緭鍑?QDD JSON锛?
[鍦ㄨ繖閲岀矘璐翠綘鐨勫墽鎯呭ぇ绾层€佷换鍔¤璁℃枃妗ｇ瓑鍐呭]`;

function openAiImportPanel() {
  let overlay = document.getElementById('ai-import-overlay');
  if (!overlay) {
    overlay = document.createElement('div');
    overlay.id = 'ai-import-overlay';
    overlay.className = 'ai-import-overlay';
    overlay.innerHTML = `
      <div class="ai-import-panel">
        <div class="ai-import-header">
          <div class="ai-import-title">
            <span>馃 AI 瀵煎叆鍔╂墜</span>
            <small>澶嶅埗鎻愮ず璇?鈫?鍙戠粰AI 鈫?绮樿创JSON 鈫?瀵煎叆</small>
          </div>
          <button class="ai-import-close" onclick="closeAiImportPanel()">脳</button>
        </div>
        <div class="ai-import-body">
          <div class="ai-import-col">
            <div class="ai-col-header">
              <span>鈶?澶嶅埗鎻愮ず璇嶏紝鍙戠粰 AI</span>
              <button class="ai-copy-btn" onclick="copyAiPrompt()">馃搵 澶嶅埗鍏ㄩ儴</button>
            </div>
            <pre class="ai-prompt-pre" id="ai-prompt-content">${escHtml(AI_PROMPT_TEMPLATE)}</pre>
          </div>
          <div class="ai-import-col">
            <div class="ai-col-header">
              <span>鈶?绮樿创 AI 杈撳嚭鐨?JSON锛岀偣鍑诲鍏?/span>
              <div class="ai-import-mode-row">
                <label><input type="radio" name="ai-import-mode" value="add" checked> 杩藉姞鍒板綋鍓?QDD</label>
                <label><input type="radio" name="ai-import-mode" value="patch"> 淇敼鎸囧畾鐜妭</label>
                <label><input type="radio" name="ai-import-mode" value="new"> 鍒涘缓涓烘柊 QDD</label>
              </div>
              <div class="ai-import-mode-hint" id="ai-import-mode-hint"></div>
            </div>
            <textarea class="ai-json-input" id="ai-json-input" placeholder='绮樿创 AI 杈撳嚭鐨?JSON 鍒拌繖閲?..\n\n鏀寔鏍煎紡锛歕n鈥?鍗曚釜 QDD 瀵硅薄锛歿 "title": "...", "steps": [...] }\n鈥?澶氫釜 QDD 鏁扮粍锛歔{ "title": "...", "steps": [...] }, ...]'></textarea>
            <div class="ai-import-actions">
              <button class="ai-validate-btn" onclick="validateAiJson()">馃攳 楠岃瘉鏍煎紡</button>
              <button class="ai-do-import-btn" onclick="doAiImport()">猬囷笍 瀵煎叆</button>
            </div>
            <div class="ai-import-feedback" id="ai-import-feedback"></div>
          </div>
        </div>
      </div>
    `;
    document.body.appendChild(overlay);
    overlay.addEventListener('click', e => { if (e.target === overlay) closeAiImportPanel(); });
  }
  overlay.classList.add('visible');

  // Bind mode radio hint
  overlay.querySelectorAll('input[name="ai-import-mode"]').forEach(radio => {
    radio.addEventListener('change', updateAiModeHint);
  });
  updateAiModeHint();
}

function updateAiModeHint() {
  const hint = document.getElementById('ai-import-mode-hint');
  if (!hint) return;
  const mode = document.querySelector('input[name="ai-import-mode"]:checked')?.value;
  const msgs = {
    add:   '',
    patch: '馃挕 鎸夌幆鑺傜紪鍙凤紙index锛夋垨鍚嶇О鍖归厤锛屽彧鏇存柊 JSON 閲屾湁鍊肩殑瀛楁锛屽師鏈夊浘鐗?绌哄瓧娈典繚鐣欎笉鍙橈紱鎵句笉鍒板尮閰嶅垯杩藉姞涓烘柊鐜妭銆侸SON 涓瘡涓幆鑺傞渶鍖呭惈 "index" 鎴?"name" 瀛楁鐢ㄤ簬瀹氫綅銆?,
    new:   '',
  };
  hint.textContent = msgs[mode] || '';
  hint.style.display = msgs[mode] ? 'block' : 'none';
}

function escHtml(str) {
  return str.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
}

function closeAiImportPanel() {
  const overlay = document.getElementById('ai-import-overlay');
  if (overlay) overlay.classList.remove('visible');
}

function copyAiPrompt() {
  navigator.clipboard.writeText(AI_PROMPT_TEMPLATE).then(() => {
    const btn = document.querySelector('.ai-copy-btn');
    if (btn) { btn.textContent = '鉁?宸插鍒讹紒'; setTimeout(() => { btn.textContent = '馃搵 澶嶅埗鍏ㄩ儴'; }, 2000); }
  }).catch(() => {
    const el = document.createElement('textarea');
    el.value = AI_PROMPT_TEMPLATE;
    document.body.appendChild(el);
    el.select();
    document.execCommand('copy');
    document.body.removeChild(el);
    showToast('鎻愮ず璇嶅凡澶嶅埗');
  });
}

function parseAiJsonInput() {
  const raw = document.getElementById('ai-json-input')?.value?.trim() || '';
  if (!raw) return { error: '璇峰厛绮樿创 AI 杈撳嚭鐨?JSON' };
  let cleaned = raw.replace(/^```(?:json)?\s*/i, '').replace(/\s*```\s*$/, '').trim();
  let parsed;
  try { parsed = JSON.parse(cleaned); }
  catch (e) { return { error: `JSON 瑙ｆ瀽澶辫触锛?{e.message}` }; }
  let qdds = [];
  if (Array.isArray(parsed)) {
    qdds = parsed;
  } else if (parsed && typeof parsed === 'object') {
    if (parsed.qdds && Array.isArray(parsed.qdds)) {
      qdds = parsed.qdds;
    } else if (parsed.steps || parsed.title) {
      qdds = [parsed];
    } else {
      return { error: '鏍煎紡涓嶈瘑鍒細闇€瑕佸寘鍚?"title" 鍜?"steps" 瀛楁' };
    }
  } else {
    return { error: '鏍煎紡涓嶈瘑鍒細闇€瑕?JSON 瀵硅薄鎴栨暟缁? };
  }
  for (const q of qdds) {
    if (!Array.isArray(q.steps)) return { error: `QDD "${q.title||'鏈懡鍚?}" 缂哄皯 steps 鏁扮粍` };
  }
  return { qdds };
}

function setAiFeedback(msg, isError) {
  const el = document.getElementById('ai-import-feedback');
  if (!el) return;
  el.textContent = msg;
  el.className = 'ai-import-feedback ' + (isError ? 'ai-feedback-error' : 'ai-feedback-ok');
}

function validateAiJson() {
  const result = parseAiJsonInput();
  if (result.error) {
    setAiFeedback('鉂?' + result.error, true);
  } else {
    const totalSteps = result.qdds.reduce((s, q) => s + (q.steps?.length || 0), 0);
    setAiFeedback(`鉁?鏍煎紡姝ｇ‘锛?{result.qdds.length} 涓?QDD锛屽叡 ${totalSteps} 涓幆鑺傦紝鍙互瀵煎叆`, false);
  }
}

function doAiImport() {
  const result = parseAiJsonInput();
  if (result.error) { setAiFeedback('鉂?' + result.error, true); return; }
  const mode = document.querySelector('input[name="ai-import-mode"]:checked')?.value || 'new';
  const { qdds } = result;
  pushHistory();

  if (mode === 'patch') {
    // 鈹€鈹€ 淇敼鎸囧畾鐜妭妯″紡 鈹€鈹€
    const currentQdd = getCurrentQdd();
    if (!currentQdd) { setAiFeedback('鉂?褰撳墠娌℃湁鎵撳紑鐨?QDD锛岃鍏堥€夋嫨鎴栧垱寤轰竴涓?, true); return; }
    const srcSteps = (qdds[0].steps || []);
    let matched = 0, added = 0;
    srcSteps.forEach(incoming => {
      // Match by index field first, then by name
      let target = null;
      const idxStr = incoming.index != null ? String(incoming.index).trim() : '';
      if (idxStr) {
        target = currentQdd.steps.find(s => String(s.index || '').trim() === idxStr);
      }
      if (!target && incoming.name) {
        target = currentQdd.steps.find(s => s.name === incoming.name);
      }
      if (target) {
        mergeAiStep(target, incoming);
        matched++;
      } else {
        // No match 鈫?append as new step
        currentQdd.steps.push(normalizeAiStep(incoming));
        added++;
      }
    });
    syncStateFromQdd(currentQdd);
    saveAllQdds();
    renderAll();
    const msg = `鉁?宸叉洿鏂?${matched} 涓幆鑺?{added > 0 ? `锛岃拷鍔?${added} 涓柊鐜妭` : ''}`;
    setAiFeedback(msg, false);
    showToast(msg);

  } else if (mode === 'add') {
    const currentQdd = getCurrentQdd();
    if (!currentQdd) { setAiFeedback('鉂?褰撳墠娌℃湁鎵撳紑鐨?QDD锛岃鍏堥€夋嫨鎴栧垱寤轰竴涓?, true); return; }
    const srcQdd = qdds[0];
    const newSteps = (srcQdd.steps || []).map(s => normalizeAiStep(s));
    currentQdd.steps.push(...newSteps);
    syncStateFromQdd(currentQdd);
    saveAllQdds();
    renderAll();
    setAiFeedback(`鉁?宸茶拷鍔?${newSteps.length} 涓幆鑺傚埌褰撳墠 QDD`, false);
    showToast(`宸茶拷鍔?${newSteps.length} 涓幆鑺俙);

  } else {
    for (const raw of qdds) {
      const newQdd = {
        id: genId(),
        title: raw.title || '浠嶢I瀵煎叆',
        steps: (raw.steps || []).map(s => normalizeAiStep(s)),
      };
      STORE.qdds.push(newQdd);
    }
    saveAllQdds();
    setAiFeedback(`鉁?宸插垱寤?${qdds.length} 涓柊 QDD`, false);
    showToast(`宸插鍏?${qdds.length} 涓?QDD`);
    closeAiImportPanel();
    showHomePage();
  }
}

function normalizeAiStep(raw) {
  return {
    id:            genId(),
    index:         raw.index != null ? String(raw.index) : '',
    name:          raw.name         || '鏈懡鍚嶇幆鑺?,
    taskType:      raw.taskType     || '',
    trigger:       raw.trigger      || '',
    location:      raw.location     || '',
    characters:    raw.characters   || '',
    desc:          raw.desc         || '',
    images:        Array.isArray(raw.images) ? raw.images : [],
    imageUrl:      raw.imageUrl     || '',
    color:         raw.color        || '',
    colorOverride: raw.colorOverride|| '',
    customFields:  Array.isArray(raw.customFields)
      ? raw.customFields.map(f => ({ key: f.key || '', value: f.value || '' }))
      : [],
  };
}

/**
 * Merge incoming AI step data into an existing step.
 * Only fields that are non-empty in `incoming` will overwrite the existing value.
 * Fields not present or empty in incoming are left untouched (preserves images, etc.)
 */
function mergeAiStep(existing, incoming) {
  const str = v => (v != null ? String(v).trim() : '');
  if (str(incoming.index))      existing.index      = str(incoming.index);
  if (str(incoming.name))       existing.name       = str(incoming.name);
  if (str(incoming.taskType))   existing.taskType   = str(incoming.taskType);
  if (str(incoming.trigger))    existing.trigger    = str(incoming.trigger);
  if (str(incoming.location))   existing.location   = str(incoming.location);
  if (str(incoming.characters)) existing.characters = str(incoming.characters);
  if (str(incoming.desc))       existing.desc       = str(incoming.desc);
  if (str(incoming.color))      existing.color      = str(incoming.color);
  if (str(incoming.colorOverride)) existing.colorOverride = str(incoming.colorOverride);
  // Images: only overwrite if incoming has non-empty images array
  if (Array.isArray(incoming.images) && incoming.images.length > 0) {
    existing.images = incoming.images;
  }
  if (str(incoming.imageUrl) && (!existing.images || existing.images.length === 0)) {
    existing.imageUrl = str(incoming.imageUrl);
  }
  // customFields: merge per key 鈥?add new keys, update existing keys if value non-empty
  if (Array.isArray(incoming.customFields)) {
    if (!Array.isArray(existing.customFields)) existing.customFields = [];
    incoming.customFields.forEach(f => {
      if (!f.key) return;
      const found = existing.customFields.find(e => e.key === f.key);
      if (found) {
        if (str(f.value)) found.value = str(f.value);
      } else {
        existing.customFields.push({ key: f.key, value: str(f.value) });
      }
    });
  }
}

// ===== JSON Backup / Restore =====
function exportBackupJson() {
  // Sync current state before export
  const qdd = getCurrentQdd();
  if (qdd) syncQddFromState(qdd);

  const payload = {
    _version: 1,
    _exportedAt: new Date().toISOString(),
    qdds: STORE.qdds,
  };
  const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' });
  const url  = URL.createObjectURL(blob);
  const a    = document.createElement('a');
  const ts   = new Date().toISOString().slice(0,16).replace(/[T:]/g, '-');
  a.href     = url;
  a.download = `QDDFlow_backup_${ts}.json`;
  a.click();
  URL.revokeObjectURL(url);
  showToast('鉁?澶囦唤宸插鍑?);
